const treeTopics = [
    {
      id: "binary-tree",
      name: "Binary Tree",
      description: "A binary tree is a data structure where each node has at most two children, referred to as the left child and the right child.",
      details: `
        - Binary search trees (BSTs) are a type of binary tree where the left child node is smaller than the parent, and the right child node is larger.
        - Common operations on binary trees include insertion, deletion, and traversal (in-order, pre-order, and post-order).
      `
    }
  ];
  
  export default treeTopics;
  